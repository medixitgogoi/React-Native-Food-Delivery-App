import { View, Text } from 'react-native';

const OrderHistory = () => {
    return (
        <View>
            <Text style={{ color: '#000' }}>OrderHistory</Text>
        </View>
    )
}

export default OrderHistory;